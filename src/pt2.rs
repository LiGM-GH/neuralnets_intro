use anyhow::Result;
use ndarray::Array1;

const N: usize = 2;

pub fn main() -> Result<()> {
    let arr: Array1<f64> = Array1::from_elem(N, 0.0);
    todo!("Don't know what this needs to do");
    Ok(())
}

